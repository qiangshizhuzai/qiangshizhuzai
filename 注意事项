采用3种方法：
(1)使用栈：
新建两个栈（先进后出），分别存放数组遍历（从后向前）的奇数和偶数；
然后奇数栈出栈，然后偶数栈出栈，分别依次存放在原数组中.
(注意:出栈时for循环的条件2不能采用stack.size()，因为栈的长度随着出栈在不断变化)
(2)使用队列：
建立arraylist，依次存放数组从前到后遍历的奇数和偶数；
将arraylist对应的元素赋值给原数组相应位置；
(此方法最直白，且不易出错)
(3)使用交换(若出现先偶后奇则交换顺序，采用类似直接插入排序思想，使用两层for循环)
以数组中的奇数为研究对象，依次与前面的元素比较，若前面元素中含偶数，则交换顺序。
(此方法中if条件选择和交换的写法易出错)
